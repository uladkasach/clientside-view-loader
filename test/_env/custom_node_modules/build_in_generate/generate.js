var view_loader = require("clientside-view-loader");
var generate = async function(dom, options){
    var child_build_dom = await view_loader.load(window.clientside_require.modules_root + "/dom_only").build();
    dom.appendChild(child_build_dom);
    return dom;
}
module.exports = generate;
